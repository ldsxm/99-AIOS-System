# AIOS 2.0｜统一智能体总控台

## 项目定位

AIOS 是 ChatGPT 与 Codex 共用的统一入口，负责理解意图、识别项目、读取规则、选择能力、调度执行、检查结果和沉淀状态。

总控台不要求用户记住内部流程。用户可以用自然语言表达目标，系统在已授权范围内自动完成路由与执行。

## 核心架构

```text
AIOS 总控台
├── Router｜项目与任务路由
├── Rules｜规则中心，单一事实来源
├── Memory｜资料、偏好与历史成果
├── Skills｜能力编排
├── Connectors｜外部服务连接
├── ChatGPT Engine｜理解、研究、创意、协调
├── Codex Engine｜文件、代码、仓库、验证
├── Workers｜luna_worker 等可插拔执行单元
├── Project Manager｜状态、成果与版本
└── Review｜检查、复盘与持续优化
```

## 引擎分工

- ChatGPT：理解与判断、规划、研究、内容、视觉方向、策略、连接器调用和项目协调。
- Codex：本地文件、代码、配置、批量处理、测试、Git/GitHub 与需要真实执行环境的任务。
- Worker：执行边界清晰、可复用、可独立验证的子任务；`luna_worker` 是可接入的示例。

## 设计原则

1. 只有一个总控台，覆盖 ChatGPT 与 Codex。
2. 规则和项目资料只有一个权威来源，避免跨平台漂移。
3. 可随时修改、升级、替换和扩展，不把临时决定写死在系统里。
4. 默认直接推进；只有关键条件确实无法推断时才提问。
5. 交互自然、清楚、少术语，体现智能体的主动判断与执行。

## 当前版本

- 版本：2.0.0
- 状态：基础协议已落地，等待按真实项目持续接入与迭代。
- 版本策略：规则变化更新 `CHANGELOG.md`；重大架构变化提升主版本；兼容性变化提升次版本；文字与流程修订提升修订号。
